rule "Deane On"
when
	Item DeaneLampScene received update
then
	var aCommand = 5;
	if (DeaneLampScene.state != 100)
	{
		aCommand = DeaneLampScene.state;
	}
	logWarn("Bigboxer23", "Bedtime Deane Lamp On")
	DeaneLamp.sendCommand(aCommand)
end